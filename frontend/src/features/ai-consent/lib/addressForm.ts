/**
 * Чи потрібна форма звертання для цієї мови.
 *
 * Живе окремо від контрола навмисно: умову «коли показувати» перевіряють двоє — сам пікер і
 * екран згоди (там від неї залежить, чи активна кнопка). Якби вона лишилась усередині
 * компонента, ці двоє з часом розійшлися б, і кнопка блокувалась би там, де питання не видно.
 *
 * Тільки українська: в англійському звертанні на «you» граматичного роду немає.
 */
export const addressFormNeeded = (language: string): boolean => language.startsWith("uk");
