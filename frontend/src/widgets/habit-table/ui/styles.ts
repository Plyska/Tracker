/**
 * Висота таблиці для місяця → внутрішній скрол замість прокрутки сторінки.
 * Відступ компенсує chrome навколо таблиці (хедер + паддінги main + тулбар + gap).
 * Мобільний: тулбар переноситься у 3 рядки (вищий) → більший відступ;
 * від `sm` тулбар стає одним рядком → відступ менший.
 */
export const BOUND_HEIGHT_CLASS =
  "max-h-[calc(100dvh-18rem)] sm:max-h-[calc(100dvh-12rem)]";
