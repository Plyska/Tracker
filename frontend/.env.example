# Базовий URL REST API. Фаза 9 працює на мок-baseQuery (in-memory) і це значення ігнорує;
# backend-фаза підставить його у fetchBaseQuery (єдина точка зміни шва мок → HTTP).
VITE_API_URL=http://localhost:3000/api
