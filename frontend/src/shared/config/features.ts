/**
 * Прапорці незавершених функцій.
 *
 * Тут лише те, що **свідомо приховано до готовності**, а не вимкнено назавжди. Код фічі лишається
 * на місці й компілюється: повернути = перемкнути рядок, а не відновлювати з історії git. Різниця
 * важить, коли фіча повертається через місяці — до того часу видалений код уже конфліктує з усім,
 * що навколо змінилось.
 *
 * Це НЕ система entitlements: тарифні обмеження живуть у `shared/lib/entitlements`
 * (`useEntitlement`) і залежать від користувача. Тут — стан продукту, однаковий для всіх.
 */

/**
 * Вільна розмова з помічником (`/assistant/chat`).
 *
 * Сторінка `/assistant` працює як раніше — чек-ін, підказки, листи-рефлексії лишаються. Прихована
 * саме **розмова**: на відміну від решти, вона приймає довільний текст і віддає його моделі разом
 * із контекстом акаунта, тобто це інший клас ризику (prompt injection, спроби дістати чужий
 * контекст, накрутка рахунку за токени). Вмикати, коли для цього буде окремий захист.
 *
 * **Справжній гейт — на сервері** (`AI_CHAT_ENABLED`, `POST /ai/chat` → 403 `AI_CHAT_DISABLED`).
 * Цей прапорець лише прибирає з інтерфейсу те, що однаково не спрацює: приховати кнопку — не
 * захист. Перемикати обидва разом.
 */
export const ASSISTANT_CHAT_ENABLED = false;

/**
 * Блок «Віджети статистики» в Налаштуваннях — вибір, які метрики й блоки показувати на
 * сторінці статистики.
 *
 * Прихований: налаштування дрібного калібру, яке роздуває екран Налаштувань набором плиток і
 * змушує розбиратись там, де людина шукала тему й мову. Сторінка статистики показує все.
 *
 * **Разом із керуванням перестає діяти й сам стан** (`selectHiddenStatWidgets`). Це не деталь:
 * `hiddenStatWidgets` живе в localStorage і в БД, тож у того, хто встиг щось приховати, віджети
 * зникли б назавжди — керування, яким їх повернути, більше немає на екрані.
 */
export const STAT_WIDGET_SETTINGS_ENABLED = false;
