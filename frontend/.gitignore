# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env (keep .env.example tracked)
.env
.env.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static

# Sentry Config File
.env.sentry-build-plugin
# Деякі версії майстра кладуть auth-токен сюди — теж секрет.
.sentryclirc

# Виняток НАПРИКІНЦІ навмисно: у git виграє останній збіг, а `vercel link` любить дописувати
# в кінець голе `.env*`, яке інакше перекриває це правило й ховає приклад конфігурації з репо.
.env*
!.env.example
